%h2 Books
%table.table.table-striped
  %thead
    %tr
      %th Title
      %th Author
      %th Publisher
      %th Genre
  %tbody
    - @books.each do |book|
      %tr
        %td= link_to book.title, book_path(book.id)
        %td= book.author
        %td= book.publisher
        %td= book.genre